{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>Pantrist-dev/renovate-config"],
  "packageRules": [
    {
      "description": "eslint 10 breaks eslint-plugin-n8n-nodes-base (uses removed context.getFilename API) via @n8n/node-cli; wait for node-cli to support it",
      "matchPackageNames": ["eslint"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    }
  ]
}
